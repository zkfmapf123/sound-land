import { SearchContainer,SearchColumn, SearchLabel } from "./Style";

interface Props{

};

const Search = ({} : Props) =>{
    
    return(
        <SearchContainer>
            <SearchLabel>{'검색결과'}</SearchLabel>
            <SearchColumn>

            </SearchColumn>
        </SearchContainer>
    )
};

export default Search;